export default {
    tooltip: {
        show: false,
        offsets: {
            top: 0,
            left: 0
        },
    },

    legend: {
        show: false,
    },
    style: {
        normal: {
            radius: 15, // 半径
            minOpacity: 0, // 最小透明度
            maxOpacity: 1, // 最大透明度
            minValue: 0,
            maxValue: 0,
            minScope: 0, // 最小区间,小于此区间的不显示
            maxScope: 1, // 最大区间,大于此区间的不显示
        }
    },
    data: []
};